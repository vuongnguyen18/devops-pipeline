services:
  app:
    build: .
    image: devops-starter:staging
    ports: ["8083:8080"]
    environment:
      - NODE_ENV=staging
      - JWT_SECRET=changeme-staging
    volumes:
      - ./data:/app/data
